program prog24 is
	number, count, total, average : real;
	procedure newNumber(n: value real) is
	begin
		count := count + 1.5;
		total := total + n;
	end;
begin
	count := 0;
	total := 0;
	loop
		write "Enter a number, 0 to exit: ";
		read number ;
		exit when number = 0;
		newNumber(number);
	end loop;
	writeln "Count is " & real2string(count);
	writeln "Total is " & real2string(total);
	if count <> 0 then
		average := total / count;
		writeln "Average is " & real2string(average);
	end if;
end;

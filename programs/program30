-- program30 should generate 10 errors
program prog30 is

    function double (n1: integer) return integer is -- 7 errors because lack of parameter kind
    begin
        return n1 + n1;
    end;


begin
	for i in 1..5
	loop
        writeln i, "  ", double(i);
	end loop;
ed prog30; -- error because typo in end

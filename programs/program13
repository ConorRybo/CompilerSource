-- procedure is missing identifier
program prog13 is
        number, newNumber: integer;
        procedure is
        begin
                n2 := n1 + n1;
        end;
begin
        write ("Enter a number: ");
        read (number);
        double(number,newNumber);
        writeln ("Number doubled is " & int2string(newNumber));
end;
